/// The controls panel.
///
/// Drawn in front of the volume panel (-150) and the fullscreen button (-160),
/// because it is the wider of the two panels and they overlap; behind the day
/// scrubber (-200), which is a development tool and stays on top while it
/// exists.
depth = -170;

help_init();
