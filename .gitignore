standard.md

# GameMaker keeps the previous generation of a regenerated resource beside the
# new one. Build leftovers, not source.
*.old.yy
*.old.png

# Playable builds. 42MB of zips and a data.win, regenerated every time and
# nothing anybody needs from source control.
builds/
